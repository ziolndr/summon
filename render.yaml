services:
  - type: web
    name: summon
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash render-start.sh
    healthCheckPath: /health
    autoDeploy: true
